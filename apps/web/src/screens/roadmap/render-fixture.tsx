import type { ScreenData } from './view';

export const ready = { "view": "ready", "props": { "actor": { "userId": 'synthetic-identity', "orgId": 'synthetic-identity' }, "mapped": [], "role": "owner" } } satisfies ScreenData;
