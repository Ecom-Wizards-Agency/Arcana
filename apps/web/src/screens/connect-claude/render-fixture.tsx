import { org } from '../synthetic-render-fixtures';
import type { ScreenData } from './view';

export const ready = { "view": "ready", "props": { "endpoint": null, "keys": [], "profiles": [], "canManage": false, "org": org } } satisfies ScreenData;
